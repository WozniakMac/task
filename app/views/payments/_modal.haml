.modal.fade{"aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "-1", :id => "payments_#{student.id}"}
  .modal-dialog{:role => "document"}
    .modal-content
      .modal-header
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
        %h4#myModalLabel.modal-title= t('navbar.payments')
      .modal-body
        %table.table.table-condensed
          %thead
            %tr
              %th= t('payment.columns.payment_date')
              %th= t('payment.columns.paid')
              %th= t('shared.actions')
          %tbody
            = render student.payments, student: student